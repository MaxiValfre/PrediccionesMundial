name: Update Data

on:
  schedule:
    - cron: '*/7 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-data
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Refresh tournament data and dashboard
        env:
          THESPORTSDB_API_KEY: ${{ secrets.THESPORTSDB_API_KEY }}
          THESPORTSDB_LEAGUE_ID: ${{ secrets.THESPORTSDB_LEAGUE_ID }}
          THESPORTSDB_REFERENCE_DATE: ${{ secrets.THESPORTSDB_REFERENCE_DATE }}
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: python run_predictions.py --update

      - name: Commit and push updated artifacts
        shell: bash
        env:
          THESPORTSDB_API_KEY: ${{ secrets.THESPORTSDB_API_KEY }}
          THESPORTSDB_LEAGUE_ID: ${{ secrets.THESPORTSDB_LEAGUE_ID }}
          THESPORTSDB_REFERENCE_DATE: ${{ secrets.THESPORTSDB_REFERENCE_DATE }}
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data dashboard docs
          git commit -m "chore(data): refresh world cup predictions"
          if git push origin HEAD:main; then
            exit 0
          fi
          echo "Push lost race, regenerating on latest main"
          git fetch origin main
          git reset --hard origin/main
          python run_predictions.py --update
          if git diff --quiet; then
            echo "No changes left after rerun"
            exit 0
          fi
          git add data dashboard docs
          git commit -m "chore(data): refresh world cup predictions"
          git push origin HEAD:main
